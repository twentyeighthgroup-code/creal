<!DOCTYPE html>
<html lang="{{ session.get('language', 'ru') }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% if session.get('language', 'ru') == 'ru' %}Добро пожаловать в Creal{% else %}Welcome to Creal{% endif %}</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600;700&family=Space+Grotesk:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  <style>
    :root {
      --primary-dark: #0a0e17;
      --secondary-dark: #121a2a;
      --tertiary-dark: #1a2439;
      --accent-blue: #4cc9f0;
      --accent-purple: #7209b7;
      --accent-pink: #f72585;
      --accent-yellow: #f8961e;
      --text-light: #f8f9fa;
      --text-gray: #adb5bd;
      --success: #4caf50;
      --warning: #ff9800;
      --error: #f44336;
      --glass-bg: rgba(18, 28, 44, 0.7);
      --glass-border: 1px solid rgba(128, 128, 128, 0.1);
      --shadow-sm: 0 4px 8px rgba(0, 0, 0, 0.1);
      --shadow-md: 0 8px 24px rgba(0, 0, 0, 0.2);
      --shadow-lg: 0 16px 48px rgba(0, 0, 0, 0.3);
      --border-radius: 16px;
      --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }
    body {
      font-family: 'Inter', sans-serif;
      background: linear-gradient(135deg, var(--primary-dark), var(--secondary-dark));
      color: var(--text-light);
      min-height: 100vh;
      overflow-x: hidden;
      position: relative;
    }
    body::before {
      content: '';
      position: fixed;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: radial-gradient(circle at center, rgba(76, 201, 240, 0.1) 0%, transparent 70%);
      z-index: -2;
    }
    #particles-js {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      z-index: -1;
    }
    .app-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 2rem;
    }
    /* --- Navigation --- */
    nav {
      position: fixed;
      top: 20px;
      left: 50%;
      transform: translateX(-50%);
      width: 95%;
      max-width: 1200px;
      background: var(--glass-bg);
      backdrop-filter: blur(16px);
      border: var(--glass-border);
      border-radius: var(--border-radius);
      padding: 1rem 2rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      z-index: 1000;
      box-shadow: var(--shadow-md);
    }
    .nav-brand {
      font-family: 'Space Grotesk', sans-serif;
      font-weight: 700;
      font-size: 1.8rem;
      color: var(--accent-blue);
      text-shadow: 0 0 10px var(--accent-blue);
      letter-spacing: 1px;
    }
    .nav-links {
      display: flex;
      gap: 1.5rem;
    }
    .nav-links a {
      text-decoration: none;
      color: var(--text-light);
      font-weight: 600;
      padding: 0.5rem 1rem;
      border-radius: 50px;
      transition: var(--transition);
      position: relative;
      overflow: hidden;
    }
    .nav-links a::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 50%;
      width: 0;
      height: 2px;
      background: var(--accent-pink);
      transition: var(--transition);
      transform: translateX(-50%);
    }
    .nav-links a:hover::after {
      width: 70%;
    }
    .nav-links a:hover {
      color: var(--accent-pink);
      background: rgba(114, 9, 183, 0.1);
    }
    .nav-links a.active {
      background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
      color: white;
      box-shadow: 0 0 15px rgba(76, 201, 240, 0.5);
    }
    .lang-switcher {
      display: flex;
      gap: 0.5rem;
    }
    .lang-btn {
      background: var(--tertiary-dark);
      border: var(--glass-border);
      color: var(--text-light);
      padding: 0.5rem 1rem;
      border-radius: 50px;
      cursor: pointer;
      transition: var(--transition);
      font-weight: 600;
    }
    .lang-btn.active {
        background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
        color: white;
    }
    .lang-btn:hover {
      background: var(--accent-purple);
      color: white;
    }
    .mobile-menu-btn {
      display: none;
      background: none;
      border: none;
      color: var(--text-light);
      font-size: 1.5rem;
      cursor: pointer;
    }
    /* --- Main Content Sections --- */
    .main-content {
      margin-top: 120px;
    }
    .section {
      display: none;
      opacity: 0;
      transform: translateY(20px);
      animation: fadeInUp 0.6s ease forwards;
    }
    .section.active {
      display: block;
      opacity: 1;
      transform: translateY(0);
    }
    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    .section-header {
      text-align: center;
      margin-bottom: 3rem;
    }
    .section-title {
      font-family: 'Space Grotesk', sans-serif;
      font-size: 2.8rem;
      margin-bottom: 1rem;
      background: linear-gradient(90deg, var(--accent-blue), var(--accent-pink));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      text-fill-color: transparent;
      text-shadow: 0 0 20px rgba(76, 201, 240, 0.3);
    }
    .section-subtitle {
      font-size: 1.2rem;
      color: var(--text-gray);
      max-width: 600px;
      margin: 0 auto;
    }
    /* --- Cards --- */
    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
      gap: 2rem;
      margin-top: 2rem;
    }
    .card {
      background: var(--glass-bg);
      backdrop-filter: blur(12px);
      border: var(--glass-border);
      border-radius: var(--border-radius);
      overflow: hidden;
      transition: var(--transition);
      box-shadow: var(--shadow-sm);
      position: relative;
      z-index: 1;
    }
    .card::before {
      content: '';
      position: absolute;
      top: -2px; left: -2px; right: -2px; bottom: -2px;
      background: linear-gradient(45deg, var(--accent-blue), var(--accent-purple), var(--accent-pink), var(--accent-yellow));
      z-index: -1;
      border-radius: calc(var(--border-radius) + 2px);
      opacity: 0;
      transition: var(--transition);
    }
    .card:hover::before {
        opacity: 1;
    }
    .card:hover {
      transform: translateY(-10px) scale(1.02);
      box-shadow: var(--shadow-lg);
    }
    .card-video, .card-news, .card-music {
        height: 100%;
    }
    .card-video video, .card-news img {
      width: 100%;
      height: 180px;
      object-fit: cover;
    }
    .card-news img {
        border-bottom: 1px solid rgba(255,255,255,0.1);
    }
    .card-content {
      padding: 1.5rem;
    }
    .card-content h3 {
      font-size: 1.3rem;
      margin-bottom: 0.5rem;
      color: var(--accent-blue);
    }
    .card-content p {
      font-size: 0.95rem;
      color: var(--text-gray);
      margin-bottom: 1rem;
      line-height: 1.5;
    }
    .card-actions {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }
    .btn {
      background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
      color: white;
      border: none;
      padding: 0.7rem 1.5rem;
      border-radius: 50px;
      cursor: pointer;
      font-weight: 600;
      transition: var(--transition);
      box-shadow: 0 4px 15px rgba(114, 9, 183, 0.3);
    }
    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 20px rgba(114, 9, 183, 0.5);
    }
    .btn-secondary {
        background: var(--tertiary-dark);
    }
    .btn-secondary:hover {
        background: var(--secondary-dark);
    }
    /* --- Forms --- */
    .upload-form {
      background: var(--glass-bg);
      backdrop-filter: blur(12px);
      border: var(--glass-border);
      border-radius: var(--border-radius);
      padding: 2rem;
      margin-bottom: 3rem;
      box-shadow: var(--shadow-md);
    }
    .form-group {
      margin-bottom: 1.5rem;
    }
    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 600;
      color: var(--text-light);
    }
    .form-control {
      width: 100%;
      padding: 0.9rem 1.2rem;
      background: rgba(10, 14, 23, 0.5);
      border: 1px solid rgba(255,255,255,0.1);
      border-radius: 10px;
      color: var(--text-light);
      font-size: 1rem;
      transition: var(--transition);
    }
    .form-control:focus {
      outline: none;
      border-color: var(--accent-blue);
      box-shadow: 0 0 0 3px rgba(76, 201, 240, 0.2);
    }
    /* --- Profile --- */
    .profile-header {
        text-align: center;
        margin-bottom: 3rem;
    }
    .avatar {
        width: 150px;
        height: 150px;
        border-radius: 50%;
        background: linear-gradient(45deg, var(--accent-blue), var(--accent-purple));
        margin: 0 auto 1.5rem;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 4rem;
        color: white;
        box-shadow: 0 10px 30px rgba(114, 9, 183, 0.4);
    }
    .profile-stats {
        display: flex;
        justify-content: center;
        gap: 2rem;
        margin-bottom: 2rem;
    }
    .stat-box {
        text-align: center;
    }
    .stat-value {
        font-size: 2rem;
        font-weight: 700;
        color: var(--accent-blue);
    }
    .stat-label {
        font-size: 0.9rem;
        color: var(--text-gray);
    }
    .profile-tabs {
        display: flex;
        justify-content: center;
        gap: 1rem;
        margin-bottom: 2rem;
    }
    .tab-btn {
        background: var(--tertiary-dark);
        border: var(--glass-border);
        color: var(--text-light);
        padding: 0.8rem 1.5rem;
        border-radius: 50px;
        cursor: pointer;
        transition: var(--transition);
        font-weight: 600;
    }
    .tab-btn.active {
        background: linear-gradient(90deg, var(--accent-blue), var(--accent-purple));
        color: white;
    }
    .tab-btn:hover:not(.active) {
        background: var(--secondary-dark);
    }
    .tab-content {
        display: none;
    }
    .tab-content.active {
        display: block;
        animation: fadeIn 0.4s ease;
    }
    @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
    }
    /* --- Back to Top Button --- */
    .back-to-top {
      position: fixed;
      bottom: 30px;
      right: 30px;
      width: 60px;
      height: 60px;
      background: linear-gradient(135deg, var(--accent-blue), var(--accent-purple));
      border: none;
      border-radius: 50%;
      color: white;
      font-size: 1.5rem;
      cursor: pointer;
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 20px rgba(114, 9, 183, 0.5);
      transition: var(--transition);
      z-index: 100;
    }
    .back-to-top:hover {
      transform: translateY(-5px) scale(1.1);
      box-shadow: 0 6px 25px rgba(114, 9, 183, 0.7);
    }
    /* --- Responsive --- */
    @media (max-width: 768px) {
      nav {
        flex-direction: column;
        gap: 1rem;
        padding: 1rem;
      }
      .nav-links {
        display: none;
        flex-direction: column;
        width: 100%;
        gap: 0.5rem;
        margin-top: 1rem;
      }
      .nav-links.active {
        display: flex;
      }
      .mobile-menu-btn {
        display: block;
        position: absolute;
        top: 1rem;
        right: 1rem;
      }
      .card-grid {
        grid-template-columns: 1fr;
      }
      .section-title {
        font-size: 2.2rem;
      }
    }
  </style>
</head>
<body>
  <div id="particles-js"></div>
  <div class="app-container">
    <!-- Navigation -->
    <nav>
      <div class="nav-brand">Creal</div>
      <div class="nav-links" id="navLinks">
        <a href="#" onclick="showSection('home')" class="active">Home</a>
        <a href="#" onclick="showSection('videos')">Videos</a>
        <a href="#" onclick="showSection('news')">News</a>
        <a href="#" onclick="showSection('music')">Music</a>
        <a href="#" onclick="showSection('profile')">Profile</a>
      </div>
      <div class="lang-switcher">
        <button class="lang-btn {% if session.get('language', 'ru') == 'ru' %}active{% endif %}" onclick="changeLanguage('ru')">RU</button>
        <button class="lang-btn {% if session.get('language', 'ru') == 'en' %}active{% endif %}" onclick="changeLanguage('en')">EN</button>
      </div>
      <button class="mobile-menu-btn" onclick="toggleMobileMenu()">
        <i class="fas fa-bars"></i>
      </button>
    </nav>

    <!-- Main Content -->
    <div class="main-content">
      <!-- Home Section -->
      <section id="home" class="section active">
        <div class="section-header">
          <h1 class="section-title">{% if session.get('language', 'ru') == 'ru' %}Добро пожаловать в <span style="color: var(--accent-pink);">Creal</span>{% else %}Welcome to <span style="color: var(--accent-pink);">Creal</span>{% endif %}</h1>
          <p class="section-subtitle">{% if session.get('language', 'ru') == 'ru' %}Современная платформа для творческих людей.{% else %}A modern platform for creative people.{% endif %}</p>
        </div>
        <div style="text-align: center; margin-top: 3rem;">
          <button class="btn" onclick="showSection('videos')">{% if session.get('language', 'ru') == 'ru' %}Начать исследовать{% else %}Start Exploring{% endif %}</button>
        </div>
      </section>

      <!-- Videos Section -->
      <section id="videos" class="section">
        <div class="section-header">
          <h2 class="section-title">{% if session.get('language', 'ru') == 'ru' %}Короткие видео{% else %}Short Videos{% endif %}</h2>
        </div>
        {% if session.get('username') %}
        <div class="upload-form">
          <h3>{% if session.get('language', 'ru') == 'ru' %}Загрузить новое видео{% else %}Upload New Video{% endif %}</h3>
          <div class="form-group">
            <label for="videoTitle">{% if session.get('language', 'ru') == 'ru' %}Название{% else %}Title{% endif %}</label>
            <input type="text" id="videoTitle" class="form-control" placeholder="{% if session.get('language', 'ru') == 'ru' %}Введите название видео{% else %}Enter video title{% endif %}">
          </div>
          <div class="form-group">
            <label for="videoFile">{% if session.get('language', 'ru') == 'ru' %}Файл видео{% else %}Video File{% endif %}</label>
            <input type="file" id="videoFile" class="form-control" accept="video/*">
          </div>
          <button class="btn">{% if session.get('language', 'ru') == 'ru' %}Загрузить{% else %}Upload{% endif %}</button>
        </div>
        {% endif %}
        <div class="card-grid">
          <div class="card card-video">
            <video controls><source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4">{% if session.get('language', 'ru') == 'ru' %}Ваш браузер не поддерживает видео.{% else %}Your browser does not support video.{% endif %}</video>
            <div class="card-content">
              <h3>{% if session.get('language', 'ru') == 'ru' %}Путешествие в космос{% else %}Journey to Space{% endif %}</h3>
              <p>{% if session.get('language', 'ru') == 'ru' %}Увлекательное 4K видео о планетах Солнечной системы.{% else %}An exciting 4K video about the planets of the Solar System.{% endif %}</p>
              <div class="card-actions">
                <button class="btn btn-secondary"><i class="far fa-heart"></i> 120</button>
                <button class="btn"><i class="far fa-share-square"></i></button>
              </div>
            </div>
          </div>
          <div class="card card-video">
            <video controls><source src="https://www.w3schools.com/html/mov_bbb.mp4" type="video/mp4">{% if session.get('language', 'ru') == 'ru' %}Ваш браузер не поддерживает видео.{% else %}Your browser does not support video.{% endif %}</video>
            <div class="card-content">
              <h3>{% if session.get('language', 'ru') == 'ru' %}Кулинарный экспресс{% else %}Culinary Express{% endif %}</h3>
              <p>{% if session.get('language', 'ru') == 'ru' %}Как приготовить идеальный бургер за 5 минут.{% else %}How to make the perfect burger in 5 minutes.{% endif %}</p>
              <div class="card-actions">
                <button class="btn btn-secondary"><i class="far fa-heart"></i> 85</button>
                <button class="btn"><i class="far fa-share-square"></i></button>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- News Section -->
      <section id="news" class="section">
        <div class="section-header">
          <h2 class="section-title">{% if session.get('language', 'ru') == 'ru' %}Новости и статьи{% else %}News & Articles{% endif %}</h2>
        </div>
        {% if session.get('username') %}
        <div class="upload-form">
          <h3>{% if session.get('language', 'ru') == 'ru' %}Создать новую новость{% else %}Create New Article{% endif %}</h3>
          <div class="form-group">
            <label for="newsTitle">{% if session.get('language', 'ru') == 'ru' %}Заголовок{% else %}Title{% endif %}</label>
            <input type="text" id="newsTitle" class="form-control" placeholder="{% if session.get('language', 'ru') == 'ru' %}Введите заголовок{% else %}Enter title{% endif %}">
          </div>
          <div class="form-group">
            <label for="newsContent">{% if session.get('language', 'ru') == 'ru' %}Текст{% else %}Content{% endif %}</label>
            <textarea id="newsContent" class="form-control" rows="4" placeholder="{% if session.get('language', 'ru') == 'ru' %}Введите текст новости...{% else %}Enter news text...{% endif %}"></textarea>
          </div>
          <button class="btn">{% if session.get('language', 'ru') == 'ru' %}Опубликовать{% else %}Publish{% endif %}</button>
        </div>
        {% endif %}
        <div class="card-grid">
          <div class="card card-news">
            <img src="https://images.unsplash.com/photo-1518770660439-4636190af475?auto=format&fit=crop&w=600" alt="Tech News">
            <div class="card-content">
              <h3>{% if session.get('language', 'ru') == 'ru' %}Прорыв в ИИ{% else %}AI Breakthrough{% endif %}</h3>
              <p>{% if session.get('language', 'ru') == 'ru' %}Новый алгоритм ИИ может предсказывать погоду с рекордной точностью.{% else %}A new AI algorithm can predict weather with record accuracy.{% endif %}</p>
              <div class="card-actions">
                <button class="btn btn-secondary">{% if session.get('language', 'ru') == 'ru' %}Читать далее{% else %}Read More{% endif %}</button>
                <span class="date">{% if session.get('language', 'ru') == 'ru' %}15 мая{% else %}May 15{% endif %}</span>
              </div>
            </div>
          </div>
          <div class="card card-news">
            <img src="https://images.unsplash.com/photo-1470225620780-dba8ba36b745?auto=format&fit=crop&w=600" alt="Music News">
            <div class="card-content">
              <h3>{% if session.get('language', 'ru') == 'ru' %}Музыкальный фестиваль{% else %}Music Festival{% endif %}</h3>
              <p>{% if session.get('language', 'ru') == 'ru' %}Самые яркие выступления с прошедшего фестиваля.{% else %}The most brilliant performances from the recent festival.{% endif %}</p>
              <div class="card-actions">
                <button class="btn btn-secondary">{% if session.get('language', 'ru') == 'ru' %}Читать далее{% else %}Read More{% endif %}</button>
                <span class="date">{% if session.get('language', 'ru') == 'ru' %}10 мая{% else %}May 10{% endif %}</span>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Music Section -->
      <section id="music" class="section">
        <div class="section-header">
          <h2 class="section-title">{% if session.get('language', 'ru') == 'ru' %}Музыка и аудио{% else %}Music & Audio{% endif %}</h2>
        </div>
        {% if session.get('username') %}
        <div class="upload-form">
          <h3>{% if session.get('language', 'ru') == 'ru' %}Загрузить трек{% else %}Upload Track{% endif %}</h3>
          <div class="form-group">
            <label for="trackTitle">{% if session.get('language', 'ru') == 'ru' %}Название{% else %}Title{% endif %}</label>
            <input type="text" id="trackTitle" class="form-control" placeholder="{% if session.get('language', 'ru') == 'ru' %}Введите название трека{% else %}Enter track title{% endif %}">
          </div>
          <div class="form-group">
            <label for="trackArtist">{% if session.get('language', 'ru') == 'ru' %}Исполнитель{% else %}Artist{% endif %}</label>
            <input type="text" id="trackArtist" class="form-control" placeholder="{% if session.get('language', 'ru') == 'ru' %}Введите имя исполнителя{% else %}Enter artist name{% endif %}">
          </div>
          <button class="btn">{% if session.get('language', 'ru') == 'ru' %}Загрузить{% else %}Upload{% endif %}</button>
        </div>
        {% endif %}
        <div class="card-grid">
          <div class="card card-music">
            <div class="card-content">
              <h3>Neon Dreams - Cosmic Wave</h3>
              <p>Genre: Synthwave | Duration: 3:45</p>
              <audio controls style="width: 100%; margin: 1rem 0;">
                <source src="https://www.w3schools.com/html/horse.ogg" type="audio/ogg">
                {% if session.get('language', 'ru') == 'ru' %}Ваш браузер не поддерживает аудио.{% else %}Your browser does not support audio.{% endif %}
              </audio>
              <div class="card-actions">
                <button class="btn btn-secondary"><i class="far fa-heart"></i> 240</button>
                <button class="btn"><i class="far fa-download"></i></button>
              </div>
            </div>
          </div>
          <div class="card card-music">
            <div class="card-content">
              <h3>Midnight Echoes - Silent City</h3>
              <p>Genre: Downtempo | Duration: 4:20</p>
              <audio controls style="width: 100%; margin: 1rem 0;">
                <source src="https://www.w3schools.com/html/horse.ogg" type="audio/ogg">
                {% if session.get('language', 'ru') == 'ru' %}Ваш браузер не поддерживает аудио.{% else %}Your browser does not support audio.{% endif %}
              </audio>
              <div class="card-actions">
                <button class="btn btn-secondary"><i class="far fa-heart"></i> 180</button>
                <button class="btn"><i class="far fa-download"></i></button>
              </div>
            </div>
          </div>
        </div>
      </section>

      <!-- Profile Section -->
      <section id="profile" class="section">
        <div class="profile-header">
          <div class="avatar">
            <i class="fas fa-user"></i>
          </div>
          <h2 class="section-title">{{ session.get('username', 'Гость') }}</h2>
          <div class="profile-stats">
            <div class="stat-box">
              <div class="stat-value">150</div>
              <div class="stat-label">{% if session.get('language', 'ru') == 'ru' %}Подписчиков{% else %}Followers{% endif %}</div>
            </div>
            <div class="stat-box">
              <div class="stat-value">80</div>
              <div class="stat-label">{% if session.get('language', 'ru') == 'ru' %}Подписок{% else %}Following{% endif %}</div>
            </div>
            <div class="stat-box">
              <div class="stat-value">12</div>
              <div class="stat-label">{% if session.get('language', 'ru') == 'ru' %}Постов{% else %}Posts{% endif %}</div>
            </div>
          </div>
        </div>
        <div class="profile-tabs">
          <button class="tab-btn active" onclick="showTab('tab-info')">{% if session.get('language', 'ru') == 'ru' %}Информация{% else %}Info{% endif %}</button>
          <button class="tab-btn" onclick="showTab('tab-content')">{% if session.get('language', 'ru') == 'ru' %}Контент{% else %}Content{% endif %}</button>
          <button class="tab-btn" onclick="showTab('tab-settings')">{% if session.get('language', 'ru') == 'ru' %}Настройки{% else %}Settings{% endif %}</button>
        </div>
        <div class="tab-content active" id="tab-info">
            <div class="upload-form">
                <h3>{% if session.get('language', 'ru') == 'ru' %}О себе{% else %}About{% endif %}</h3>
                <p>{% if session.get('language', 'ru') == 'ru' %}Творческий человек, люблю делиться идеями и вдохновлять других.{% else %}A creative person, love to share ideas and inspire others.{% endif %}</p>
            </div>
        </div>
        <div class="tab-content" id="tab-content">
            <div class="card-grid">
                <div class="card card-news">
                    <img src="https://via.placeholder.com/300x180" alt="User Post">
                    <div class="card-content">
                        <h3>{% if session.get('language', 'ru') == 'ru' %}Мой первый пост{% else %}My First Post{% endif %}</h3>
                        <p>{% if session.get('language', 'ru') == 'ru' %}Привет, Creal! Это мой блог!{% else %}Hi, Creal! This is my blog!{% endif %}</p>
                        <div class="card-actions">
                            <button class="btn btn-secondary">{% if session.get('language', 'ru') == 'ru' %}Читать далее{% else %}Read More{% endif %}</button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="tab-content" id="tab-settings">
            <div class="upload-form">
                <h3>{% if session.get('language', 'ru') == 'ru' %}Язык интерфейса{% else %}Interface Language{% endif %}</h3>
                <div class="form-group">
                    <select class="form-control">
                        <option value="ru" {% if session.get('language', 'ru') == 'ru' %}selected{% endif %}>{% if session.get('language', 'ru') == 'ru' %}Русский{% else %}Russian{% endif %}</option>
                        <option value="en" {% if session.get('language', 'ru') == 'en' %}selected{% endif %}>{% if session.get('language', 'ru') == 'ru' %}Английский{% else %}English{% endif %}</option>
                    </select>
                </div>
                <button class="btn" onclick="logout()">{% if session.get('language', 'ru') == 'ru' %}Выйти{% else %}Log Out{% endif %}</button>
            </div>
        </div>
      </section>
    </div>
  </div>
  <button class="back-to-top" onclick="scrollToTop()"><i class="fas fa-arrow-up"></i></button>

  <script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js"></script>
  <script>
    // Инициализация частиц
    particlesJS("particles-js", {
      particles: {
        number: { value: 50, density: { enable: true, value_area: 800 } },
        color: { value: "#4cc9f0" },
        shape: { type: "circle" },
        opacity: { value: 0.3, random: true },
        size: { value: 3, random: true },
        move: {
          enable: true,
          speed: 1,
          direction: "none",
          random: true,
          straight: false,
          out_mode: "out",
          bounce: false,
        }
      },
      interactivity: {
        detect_on: "canvas",
        events: {
          onhover: { enable: true, mode: "repulse" },
          onclick: { enable: true, mode: "push" },
          resize: true
        }
      }
    });

    // Функции для навигации по секциям
    function showSection(sectionId) {
      document.querySelectorAll('.section').forEach(section => {
        section.classList.remove('active');
      });
      document.getElementById(sectionId).classList.add('active');
      
      document.querySelectorAll('.nav-links a').forEach(link => {
        link.classList.remove('active');
      });
      event.target.classList.add('active');
    }

    function toggleMobileMenu() {
        const navLinks = document.getElementById('navLinks');
        navLinks.classList.toggle('active');
    }

    function showTab(tabId) {
        document.querySelectorAll('.tab-content').forEach(tab => {
            tab.classList.remove('active');
        });
        document.getElementById(tabId).classList.add('active');
        
        document.querySelectorAll('.tab-btn').forEach(btn => {
            btn.classList.remove('active');
        });
        event.target.classList.add('active');
    }

    function changeLanguage(lang) {
        window.location.href = `/switch_language?lang=${lang}`;
    }

    function logout() {
        fetch('/logout', { method: 'POST' }).then(() => window.location.href = '/');
    }

    function scrollToTop() {
        window.scrollTo({ top: 0, behavior: 'smooth' });
    }

    // Закрытие мобильного меню при клике на ссылку
    document.querySelectorAll('.nav-links a').forEach(link => {
        link.addEventListener('click', () => {
            document.getElementById('navLinks').classList.remove('active');
        });
    });

  </script>
</body>
</html>